import { ReplaceUrlPipe } from './replace-url.pipe';

describe('ReplaceUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
